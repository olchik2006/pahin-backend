const getAllTrees = async (req, res) => {
  res.json({ message: 'getAllTrees - TODO' });
};

const getSpecies = async (req, res) => {
  res.json({ message: 'getSpecies - TODO' });
};

const getTreeById = async (req, res) => {
  res.json({ message: 'getTreeById - TODO' });
};

const plantTree = async (req, res) => {
  res.json({ message: 'plantTree - TODO' });
};

const deleteTree = async (req, res) => {
  res.json({ message: 'deleteTree - TODO' });
};

module.exports = { getAllTrees, getSpecies, getTreeById, plantTree, deleteTree };
